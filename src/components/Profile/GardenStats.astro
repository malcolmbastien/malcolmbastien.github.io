---
import { stages } from "../../lib/theme";

interface Props {
    counts: {
        all: number;
        seed: number;
        sprout: number;
        evergreen: number;
    };
}

const { counts } = Astro.props;
---

<div class="bg-white dark:bg-slate-800 p-6 rounded-lg border border-slate-200 dark:border-slate-700 shadow-sm">
    <h3 class="text-xs font-bold uppercase tracking-[0.2em] text-slate-400 mb-6">Taxonomy</h3>
    <div class="space-y-6">
        <div>
            <div class="flex justify-between items-center mb-2">
                <span class={`text-sm font-bold ${stages.seed.colors.text} uppercase tracking-[0.1em]`}>{stages.seed.label}</span>
                <span class="font-mono text-lg font-bold text-slate-700 dark:text-slate-300">{counts.seed.toString().padStart(3, '0')}</span>
            </div>
            <div class="w-full h-2 bg-slate-100 dark:bg-slate-700 rounded-full overflow-hidden mb-3">
                <div class={`${stages.seed.colors.dot} h-full rounded-full transition-all duration-500`} style={`width: ${(counts.seed / (counts.all || 1)) * 100}%`}></div>
            </div>
            <p class="text-[12px] text-slate-500 dark:text-slate-400 leading-relaxed">Raw fragments and fleeting thoughts. Fragile starting points for cultivation.</p>
        </div>
        <div>
            <div class="flex justify-between items-center mb-2">
                <span class={`text-sm font-bold ${stages.sprout.colors.text} uppercase tracking-[0.1em]`}>{stages.sprout.label}</span>
                <span class="font-mono text-lg font-bold text-slate-700 dark:text-slate-300">{counts.sprout.toString().padStart(3, '0')}</span>
            </div>
            <div class="w-full h-2 bg-slate-100 dark:bg-slate-700 rounded-full overflow-hidden mb-3">
                <div class={`${stages.sprout.colors.dot} h-full rounded-full transition-all duration-500`} style={`width: ${(counts.sprout / (counts.all || 1)) * 100}%`}></div>
            </div>
            <p class="text-[12px] text-slate-500 dark:text-slate-400 leading-relaxed">Ideas taking root and branching out. Growing structure and contextual links.</p>
        </div>
        <div>
            <div class="flex justify-between items-center mb-2">
                <span class={`text-sm font-bold ${stages.evergreen.colors.text} uppercase tracking-[0.1em]`}>{stages.evergreen.label}</span>
                <span class="font-mono text-lg font-bold text-slate-700 dark:text-slate-300">{counts.evergreen.toString().padStart(3, '0')}</span>
            </div>
            <div class="w-full h-2 bg-slate-100 dark:bg-slate-700 rounded-full overflow-hidden mb-3">
                <div class={`${stages.evergreen.colors.dot} h-full rounded-full transition-all duration-500`} style={`width: ${(counts.evergreen / (counts.all || 1)) * 100}%`}></div>
            </div>
            <p class="text-[12px] text-slate-500 dark:text-slate-400 leading-relaxed">Enduring and deeply interconnected insights. Polished, atomic notes.</p>
        </div>
    </div>
</div>
